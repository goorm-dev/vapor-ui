import { Button, Checkbox } from '@vapor-ui/core';

export const Component = () => (
    <div>
        <Button>Click</Button>
        <Checkbox.Root>
            <Checkbox.IndicatorPrimitive />
        </Checkbox.Root>
    </div>
);
