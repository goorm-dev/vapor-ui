import { Alert, Badge, Button } from '@goorm-dev/vapor-core';

export const Component = () => (
    <>
        <Alert color="primary">Alert message</Alert>
        <Badge color="primary">Badge</Badge>
        <Button>Button</Button>
    </>
);
