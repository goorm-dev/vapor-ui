import { Text } from '@goorm-dev/vapor-core';
import { Card, Badge } from '@vapor-ui/core';

export const Component = () => (
    <Card.Root>
        <Badge>New</Badge>
        <Text>Content</Text>
    </Card.Root>
);
