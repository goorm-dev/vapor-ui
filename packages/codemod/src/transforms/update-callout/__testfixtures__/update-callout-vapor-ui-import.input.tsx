// @ts-nocheck

import { HeartIcon } from '@vapor-ui/icons';
import { Alert } from '@vapor-ui/core';

export const App = () => {
    return (
        <Alert>
            <HeartIcon />
            This is an alert message
        </Alert>
    );
};
