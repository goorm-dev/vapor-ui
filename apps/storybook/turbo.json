{
    "extends": ["//"],
    "tasks": {
        "build:storybook": {
            "dependsOn": ["^build:storybook"],
            "inputs": [
                ".storybook/**",
                "../../packages/core/src/**",
                "../../packages/composites/src/**"
            ],
            "outputs": ["storybook-static/**"]
        }
    }
}
