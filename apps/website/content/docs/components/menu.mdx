---
title: 'Menu'
site_name: 'Menu - Vapor Core'
description: '드롭다운 Menu와 컨텍스트 Menu를 제공하는 컴포넌트입니다'
---

<Demo name="menu/default-menu">
```json doc-gen:file
{
    "file": "./src/components/demo/examples/menu/default-menu.tsx",
    "codeblock": true
}
```
</Demo>

## Property

---

### Disabled

Menu의 비활성화 상태를 설정합니다.

<Demo name="menu/menu-disabled">
```json doc-gen:file
{
    "file": "./src/components/demo/examples/menu/menu-disabled.tsx",
    "codeblock": true
}
```
</Demo>

## Examples

---

### Positioning

Menu의 표시 위치를 설정합니다.

<Demo name="menu/menu-positioning">
```json doc-gen:file
{
    "file": "./src/components/demo/examples/menu/menu-positioning.tsx",
    "codeblock": true
}
```
</Demo>

### Groups

Menu 아이템을 그룹화합니다.

<Demo name="menu/menu-groups">
```json doc-gen:file
{
    "file": "./src/components/demo/examples/menu/menu-groups.tsx",
    "codeblock": true
}
```
</Demo>

### With Checkbox Items

Checkbox Item을 사용합니다.

<Demo name="menu/menu-checkbox">
```json doc-gen:file
{
    "file": "./src/components/demo/examples/menu/menu-checkbox.tsx",
    "codeblock": true
}
```
</Demo>

### With Radio Items

Radio Item을 사용합니다.

<Demo name="menu/menu-radio">
```json doc-gen:file
{
    "file": "./src/components/demo/examples/menu/menu-radio.tsx",
    "codeblock": true
}
```
</Demo>

### With Submenu

Submenu를 구성할 수 있습니다.

<Demo name="menu/menu-submenu">
```json doc-gen:file
{
    "file": "./src/components/demo/examples/menu/menu-submenu.tsx",
    "codeblock": true
}
```
</Demo>

## Props Table

---

### Menu.Root

Menu의 하위 요소를 그룹화합니다. 자체 HTML 요소를 렌더링하지 않습니다.

<ComponentPropsTable file="menu" section="props" />

### Menu.Trigger

Menu를 여는 버튼입니다. `<button>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="triggerProps" />

### Menu.Popup

Menu의 컨텐츠를 표시합니다. `<PortalPrimitive>`, `<PositionerPrimitive>`, `<PopupPrimitive>`의 중첩 구조로 이루어져 있습니다.

<ComponentPropsTable file="menu" section="popupProps" />

### Menu.Item

각각의 Menu 항목입니다. `<div>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="itemProps" />

### Menu.Separator

스크린 리더가 접근 가능한 구분자입니다. `<div>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="separatorProps" />

### Menu.Group

여러 Menu Item을 그룹화합니다. `<div>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="groupProps" />

### Menu.GroupLabel

Menu Group의 레이블을 표시합니다. `<div>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="groupLabelProps" />

### Menu.SubmenuRoot

Submenu 하위의 요소를 그룹화합니다. 자체 HTML 요소를 렌더링하지 않습니다.

<ComponentPropsTable file="menu" section="submenuRootProps" />

### Menu.SubmenuPopup

Submenu의 컨텐츠를 표시합니다. `<PortalPrimitive>`, `<PositionerPrimitive>`, `<SubmenuPopupPrimitive>`의 중첩 구조로 이루어져 있습니다.

<ComponentPropsTable file="menu" section="submenuPopupProps" />

### Menu.SubmenuTriggerItem

Submenu를 여는 요소입니다. `<div>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="submenuTriggerItemProps" />

### Menu.CheckboxItem

다중 선택할 수 있는 Menu Item입니다. `<div>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="checkboxItemProps" />

### Menu.RadioGroup

Radio Item을 그룹화합니다. `<div>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="radioGroupProps" />

### Menu.RadioItem

단일 선택할 수 있는 Menu Item입니다. `<div>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="radioItemProps" />

---

### Menu.PortalPrimitive

Menu 콘텐츠를 DOM 트리의 다른 위치로 렌더링하는 컴포넌트입니다.

<ComponentPropsTable file="menu" section="portalPrimitiveProps" />

### Menu.PositionerPrimitive

트리거를 기준으로 Menu 콘텐츠의 위치를 설정합니다. `<div>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="portalPrimitiveProps" />

### Menu.CheckboxItemIndicatorPrimitive

Checkbox Item의 선택 여부를 표시합니다. `<div>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="checkboxItemIndicatorPrimitiveProps" />

### Menu.RadioItemIndicatorPrimitive

Radio Item의 선택 여부를 표시합니다. `<div>` 요소를 렌더링합니다.

<ComponentPropsTable file="menu" section="radioItemIndicatorPrimitiveProps" />
