{
    "name": "Root",
    "displayName": "Avatar.Root",
    "description": "하위 컴포넌트를 감싸는 컨테이너 역할을 합니다. `<span>` 요소를 렌더링합니다.",
    "props": [
        {
            "name": "className",
            "type": ["string", "((state: Avatar.Root.State) => string)", "undefined"],
            "required": false,
            "description": "CSS class applied to the element, or a function that\nreturns a class based on the component’s state."
        },
        {
            "name": "render",
            "type": [
                "((props: HTMLProps, state: Avatar.Root.State) => ReactElement)",
                "ReactElement",
                "undefined"
            ],
            "required": false,
            "description": "Allows you to replace the component’s HTML element\nwith a different tag, or compose it with another component.\n\nAccepts a `ReactElement` or a function that returns the element to render."
        },
        {
            "name": "src",
            "type": ["string", "undefined"],
            "required": false,
            "description": "프로필 이미지 URL"
        },
        {
            "name": "alt",
            "type": ["string"],
            "required": true,
            "description": "대체 텍스트"
        },
        {
            "name": "delay",
            "type": ["number", "undefined"],
            "required": false,
            "description": "Fallback 표시 지연 시간(ms)"
        }
    ],
    "variants": [
        {
            "name": "size",
            "values": ["sm", "md", "lg", "xl"],
            "defaultValue": "md"
        },
        {
            "name": "shape",
            "values": ["square", "circle"],
            "defaultValue": "square"
        }
    ]
}
