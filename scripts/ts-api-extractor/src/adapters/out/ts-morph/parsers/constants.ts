/**
 * Parser Layer Constants
 */

/**
 * 선언 위치 타입
 */
export enum DeclarationSourceType {
    PROJECT = 'project',
    BASE_UI = 'base-ui',
    REACT_TYPES = 'react-types',
    DOM_TYPES = 'dom-types',
    EXTERNAL = 'external',
}
